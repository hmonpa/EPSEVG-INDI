Se añade un radio button y dos funciones tipo slot para interactuar con él.
	canviaCameraOBS()
	canviaCameraEscena()

También se añaden dos signals para que tenga concordancia con el teclado.
	signalOBS()
	signalEscena()

Se juega con la variable teclaL, en función de si es 0 o 1, va pintando un foco u otro.
